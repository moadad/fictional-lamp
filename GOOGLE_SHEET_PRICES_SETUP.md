# ربط الأسعار بـ Google Sheet — Jood Orders Pro V5.2

## النتيجة المطلوبة
داخل نفس ملف Google Sheet الخاص بالنظام توجد ورقة باسم:

**الاسعار**

وتكون بهذا الشكل فقط:

| A | B |
|---|---|
| رقم الموديل | السعر |
| 371 | 140 |
| 1810 | 770 |
| 650 | 230 |

يمكن للـ Admin إدخال/تعديل الأسعار مباشرة من Google Sheet، وجميع مستخدمي الموقع سيقرأون نفس الأسعار.

## التركيب مرة واحدة
المشروع المرفوع يحتوي واجهة الموقع فقط، بينما خادم Google Apps Script الحالي منشور خارج ملفات ZIP. لذلك أُضيف ملف جاهز باسم:

`apps-script-prices-addon.gs`

1. افتح Google Sheet المستخدم حاليًا مع النظام.
2. افتح Extensions > Apps Script.
3. أضف ملف Script جديد والصق داخله كامل محتوى `apps-script-prices-addon.gs`.
4. في دالة `doGet(e)` الحالية، أضف **في أول الدالة** السطرين التاليين:

```javascript
var pricesResponse = JOOD_PRICES_handleRequest_(e);
if (pricesResponse) return pricesResponse;
```

5. احفظ المشروع ثم أعد نشر Web App من **Deploy > Manage deployments > Edit > New version > Deploy**.

## مهم
- لا تستخدم أسعار Local Storage بعد الآن.
- فاتورة الجاهز تطلب الأسعار من Google Sheet عند كل إصدار فاتورة، لذلك السعر موحد بين كل الأجهزة.
- إذا لم تكن ورقة `الاسعار` موجودة، الإضافة تنشئها تلقائيًا وتكتب العناوين في A1 وB1.
- إذا كان موديل جاهزًا وليس له سعر في الورقة، لن تصدر الفاتورة وتظهر أرقام الموديلات الناقصة.
