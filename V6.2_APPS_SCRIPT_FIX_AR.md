# إصلاح V6.2 — Google Apps Script

## سبب تعطل جميع الأزرار
عند فتح الواجهة من Google Apps Script كان `index.html` يحاول تحميل `./api.js` و `./app.js` و `./styles.css` كملفات ويب مستقلة. Google Apps Script HtmlService لا يقدّم هذه المسارات تلقائيًا، لذلك لم يتم تحميل JavaScript وكانت جميع أزرار `onclick` بلا وظائف.

## ما تم إصلاحه
- دمج `styles.css` داخل `index.html`.
- دمج `api.js` داخل `index.html`.
- دمج `app.js` داخل `index.html`.
- تعطيل Service Worker في نسخة Apps Script لأن `./sw.js` لا يُخدم عبر HtmlService.
- إزالة رابط manifest الخارجي من نسخة Apps Script.
- الإبقاء على رابط الخادم الحالي كالرابط الافتراضي.

## طريقة التركيب
في مشروع Google Apps Script: استبدل محتوى ملف HTML المسمى `index` بالكامل بمحتوى `index.html` الموجود في هذه النسخة، واستبدل `Code.gs` بالنسخة الموجودة هنا، ثم اعمل Deploy > Manage deployments > Edit > New version > Deploy.
