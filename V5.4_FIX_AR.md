# إصلاح V5.4 — الأسعار المشتركة

هذه النسخة تعالج سببين شائعين لبقاء رسالة تعذر قراءة الأسعار:

1. رابط Apps Script قديم محفوظ داخل متصفح أحد المستخدمين. عند فتح V5.4 أول مرة يتم حذف الرابط المحفوظ القديم واستخدام رابط الخادم الافتراضي الموجود في `api.js`.
2. Service Worker قديم يعرض ملفات JavaScript السابقة. تم تغيير رقم Cache إلى V5.4 وتغيير إصدارات `api.js` و`app.js` لإجبار المتصفح على تحميل الملفات الجديدة.

كما أن رسالة الخطأ الجديدة تعرض **سبب الخادم الحقيقي** و**رابط الخادم المستخدم** بدل رسالة عامة فقط.

## بعد رفع الملفات إلى GitHub Pages
استبدل ملفات `index.html` و`app.js` و`api.js` و`sw.js` من هذه النسخة. ثم افتح الموقع مرة واحدة وانتظر ثواني وأعد تحميل الصفحة.

## Apps Script
استخدم ملف `Code.gs` الموجود في هذه النسخة، ثم حدّث نفس Deployment بإصدار جديد.
