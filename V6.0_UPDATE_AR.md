# Jood Orders Pro — تحديث V6.0

## أهم التعديلات
- تسريع قراءة الخادم وتقليل الكتابات المتكررة أثناء التسليم.
- إصلاح POST مع Google Apps Script مع رجوع آمن إلى JSONP عند تعذر الاتصال.
- حماية من تكرار نفس عملية التسليم وخصم المخزون مرتين.
- تسريع البحث بفهرس مؤقت والبحث باسم العميل أو رقم الفاتورة.
- أي كمية تسليم للموديل (أقل / أكثر / مساوية للمطلوب) تعتبر الموديل مكتملًا.
- الموديل المكتمل يختفي من الطلبية النشطة ومن تشغيل المخزن.
- زر «مكتمل» يعرض العميل والموديلات التي تم تسليمها وحالة كل موديل.
- تشغيل المخزن يعرض المطلوب المتبقي فقط بعد استبعاد الموديلات المكتملة.
- المخزون يتحدث بعد التسليم، وعمود «المتبقي» = المتاح - المطلوب:
  - سالب: أحمر (عجز).
  - صفر أو موجب: أخضر (متاح).
- شاشة المخزن تعيد التحقق من بيانات الخادم عند فتحها إذا مر أكثر من 10 ثوانٍ.

## مهم لتفعيل تعديلات الخادم
ملف `Code.gs` داخل المشروع هو نسخة الخادم الجديدة. يجب تحديث مشروع Google Apps Script الحالي بهذا الملف ثم تحديث الـ Deployment الحالي. إذا تم تحديث نفس الـ Deployment فلا تحتاج عادةً إلى تغيير رابط الخادم في الواجهة.


## رابط الخادم المعتمد في نسخة V6.1
`https://script.google.com/macros/s/AKfycbxPavQC_Oq24oAoiXQ0cyCsyA2x_NdzyJALyjEA3lFd9Mrl71BAyjDT24OJP63tbNovmw/exec`

تم تغيير مفتاح ترحيل الرابط وكاش الـ PWA حتى يتم تجاهل أي رابط خادم قديم محفوظ على أجهزة المستخدمين عند تحميل النسخة الجديدة.
