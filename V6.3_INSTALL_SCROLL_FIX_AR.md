# V6.3 — إصلاح الرجوع والتثبيت

## الرجوع إلى نفس مكان العميل
- عند فتح طلبية عميل من قائمة العملاء، يحفظ البرنامج موضع التمرير الحالي تلقائيًا.
- عند الضغط على سهم الرجوع داخل الطلبية، يعود المستخدم إلى نفس مكانه في قائمة العملاء بدل الرجوع لأول الصفحة.
- تمت إضافة إعادة ضبط ذكية لموضع التمرير أكثر من مرة بعد الرسم لضمان ثبات المكان حتى لو أُعيد رسم بطاقات العملاء.

## إصلاح التثبيت PWA
- تسجيل Service Worker يبدأ فور فتح الصفحة، وليس بعد تسجيل الدخول فقط.
- إضافة زر «تثبيت البرنامج على الجهاز» داخل القائمة الجانبية.
- تحسين manifest بإضافة id وscope وأيقونات maskable وإعدادات Android/iOS الأساسية.
- تحديث إصدار cache إلى V6.3 لمنع بقاء الملفات القديمة.

## ملاحظة
التثبيت المباشر كتطبيق ويب PWA يحتاج تشغيل الواجهة من رابط HTTPS (أو localhost أثناء التطوير). إذا كانت الواجهة تعمل من Google Apps Script HtmlService فقط، فالأفضل نشر ملفات الواجهة على استضافة HTTPS تدعم الملفات الثابتة وService Worker، مع بقاء Apps Script كخادم API.
